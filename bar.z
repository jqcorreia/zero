// get external function from "libc" hence the "c" string
external "c" { 
    fn printf(format: str, ...) -> i32
}

// external fn printf(format: str, ...) -> i32
fn main() -> i32 {
    printf("Hello from bar!\n")
    return 0
}

