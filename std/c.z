external "c" {
    fn printf(format: str, ...) -> i32
    fn fopen(filename: str, mode: str) -> &FILE
    fn fputc(char: i32, file: &FILE) -> i32
    fn fputs(string: str, file: &FILE) -> u64
    fn fclose(file: &FILE) -> u64
}

struct FILE {}

