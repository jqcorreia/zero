import "std/c"
fn factorial(n: u64) -> u64 {
    if n == 0 {
        return 1
    } else {
        return n * factorial(n - 1)
    }
    return 100
}

fn main() {
    printf("Factorial of 5 is %d\n", factorial(5)) // Should print "120"
}
