import "std/c"


fn main() { 
    x: u64= 10
    y :u8= 20
    if x == 0 {
        printf("Hello!")
    }
    printf("%d\n", param_test(y, y))
}

// Different parameter types and return values
fn param_test(x: u8, y: u8) -> u8 {
    return x + y
}
