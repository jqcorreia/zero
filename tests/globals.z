import "std/c"

struct Point {
    x: i32
    y: i32
}

point := Point{ x = 100, y = 20 }
global_string := "Hello, World!"

fn main() {
    printf("Global String: %s\n", global_string)
    printf("Point: (%d, %d)\n", point.x, point.y)
}

